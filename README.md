## 🎯 Назначение проекта

**Asteroid Watch** — это комплексная система для автоматического сбора, анализа и мониторинга потенциально опасных астероидов (PHA). Система реализует двухуровневый подход к получению данных:

### Первый уровень (get_data.py):

- **Источник:** NASA SBDB API (Small-Body Database)
    
- **Запрос:** Получение всех потенциально опасных астероидов (PHA)
    
- **Результат:** ~2000-3000 объектов с полными орбитальными и физическими параметрами
    
- **Цель:** Создание базы данных опасных объектов для дальнейшего анализа
    

### Второй уровень (cad_api.py):

- **Источник:** NASA CAD API (Close Approach Data)
    
- **Запрос:** Сближения с Землёй на расстоянии ≤ 0.05 а.е.
    
- **Фильтр:** Только для PHA астероидов из первого уровня
    
- **Результат:** Только опасные сближения опасных астероидов
    
- **Цель:** Выявление конкретных угроз на ближайшие 10 лет
    

**Основные функции:**

1. Автоматический сбор данных из NASA API
    
2. Детальный анализ сближений астероидов с Землей
    
3. Автоматическая оценка уровня угрозы
    
4. Хранение и структурирование данных в PostgreSQL
    
5. REST API для доступа к данным
    

**Целевая аудитория:**

- Астрономы и исследователи космоса
    
- Организации планетарной защиты
    
- Научно-исследовательские институты
    
- Образовательные учреждения
    

---

## 🚀 Быстрый старт

### Предварительные требования

- **Python 3.10+**
    
- **PostgreSQL 14+**
    
- **Chrome/Chromium** (для Selenium в CAD API)
    
- **Учетные записи для доступа к:**
    
    - NASA SBDB API
        
    - NASA CAD API
        
    - JPL Horizons
        

### Установка

``` bash
# Клонирование репозитория
git clone <repository-url>
cd asteroid-watch

# Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

# Установка зависимостей
pip install -r requirements.txt
```

### Настройка окружения

1. Создайте файл `.env` на основе примера:

``` bash
cp .env.example .env
```

2. Настройте переменные окружения:

``` env
# Настройки PostgreSQL
POSTGRES_USER=asteroid_watch
POSTGRES_PASSWORD=your_secure_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=asteroid_watch
POSTGRES_ASYNC_DRIVER=asyncpg

# NASA API настройки (при необходимости)
NASA_API_KEY=your_nasa_api_key
```

3. Создайте базу данных:

``` bash
createdb asteroid_watch -U postgres
```

4. Примените миграции Alembic:

``` bash
alembic upgrade head
```

### Запуск системы

#### Шаг 1: Получение данных об астероидах (Первый уровень)

``` bash

python -c "from utils.get_data import NASASBDBClient; client = NASASBDBClient(); asteroids = client.get_asteroids(limit=100)"
```

#### Шаг 2: Получение данных о сближениях (Второй уровень)

``` bash
python -c "from utils.cad_api import CombinedCADClient; client = CombinedCADClient(); approaches = client.get_close_approaches(asteroid_ids=['99942', '101955'])"
```

### Используемые внешние API

1. **NASA SBDB API** — данные об астероидах
    
2. **NASA CAD API** — данные о сближениях
    
3. **JPL Horizons** — эфемериды небесных тел

### Благодарности

- NASA/JPL за предоставление данных через API
    
- Разработчикам astroquery за удобный интерфейс к астрономическим базам данных
    
- Сообществу SQLAlchemy за отличный ORM

## 🔗 Полезные ссылки

- [NASA Small-Body Database](https://ssd.jpl.nasa.gov/sbdb.cgi)
    
- [NASA CAD API Documentation](https://ssd-api.jpl.nasa.gov/doc/cad.html)
    
- [Astroquery Documentation](https://astroquery.readthedocs.io/)
    
- [SQLAlchemy Documentation](https://docs.sqlalchemy.org/)

