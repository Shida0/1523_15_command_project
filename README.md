# Asteroid Watch System

## 🎯 Назначение проекта

**Asteroid Watch** — это система для автоматического мониторинга и анализа потенциально опасных астероидов (PHA — Potentially Hazardous Asteroids). Система собирает данные из официальных источников NASA, анализирует сближения с Землей и оценивает уровень угрозы.

### Ключевые возможности:

1. **Автоматический сбор данных** из нескольких источников NASA:
   - **SBDB API** (Small-Body Database) — данные об астероидах и их орбитальных параметрах
   - **CAD API** (Close Approach Data) — данные о будущих сближениях с Землей
   - **Sentry API** — данные об объектах с ненулевой вероятностью столкновения (Sentry-II)

2. **Двухуровневая архитектура сбора данных**:
   - **Уровень 1**: Получение списка всех потенциально опасных астероидов (~2000-3000 объектов)
   - **Уровень 2**: Получение данных о сближениях только для PHA астероидов
   - **Уровень 3**: Получение данных об объектах с ненулевой вероятностью столкновения с Землей, анализ их опасности

3. **Комплексный анализ угроз**:
   - Расчет энергии потенциального удара
   - Учитывается оценка по шкалам Турина и Палермо
   - Классификация по уровню угрозы и категории воздействия
   - Локализация данных с Sentry API на русский язык 

1. **Современная технологическая стэк**:
   - Асинхронное программирование (async/await)
   - SQLAlchemy ORM с асинхронной поддержкой
   - PostgreSQL для хранения данных
   - Автоматические миграции через Alembic

### Целевая аудитория:
- Астрономы и исследователи космоса
- Организации планетарной защиты
- Научно-исследовательские институты
- Образовательные учреждения
- Разработчики приложений для мониторинга космических угроз

---

## 🚀 Быстрый старт

### Предварительные требования

- **Python 3.10+**
- **PostgreSQL 14+**
- **Chrome/Chromium** (для Selenium при работе с CAD API)
- **Доступ к NASA API** (публичный, без ключа)

### Установка


```bash
# 1. Клонирование репозитория
git clone <repository-url>
cd asteroid-watch

# 2. Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

# 3. Установка зависимостей
pip install -r requirements.txt

# 4. Настройка переменных окружения

# 5. Применение миграций базы данных
alembic upgrade head
```

### Конфигурация

Создайте файл `.env` со следующими параметрами:

```env
# Настройки базы данных PostgreSQL
POSTGRES_USER=asteroid_watch
POSTGRES_PASSWORD=your_secure_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=asteroid_watch
```

3. Создайте базу данных:

``` bash
createdb asteroid_watch -U postgres
```

4. Примените миграции Alembic:

``` bash
alembic upgrade head
```

### Запуск системы

#### Шаг 1: Получение данных об астероидах (Первый уровень)

``` bash

python -c "from utils.get_data import NASASBDBClient; client = NASASBDBClient(); asteroids = client.get_asteroids(limit=100)"
```

#### Шаг 2: Получение данных о сближениях (Второй уровень)

``` bash
python -c "from utils.cad_api import CombinedCADClient; client = CombinedCADClient(); approaches = client.get_close_approaches(asteroid_ids=['99942', '101955'])"
```

### Используемые внешние API

1. **NASA SBDB API** — данные об астероидах
    
2. **NASA CAD API** — данные о сближениях
    
3. **JPL Horizons** — эфемериды небесных тел

### Благодарности

- NASA/JPL за предоставление данных через API
    
- Разработчикам astroquery за удобный интерфейс к астрономическим базам данных
    
- Сообществу SQLAlchemy за отличный ORM

## 🔗 Полезные ссылки

- [NASA Small-Body Database](https://ssd.jpl.nasa.gov/sbdb.cgi)
    
- [NASA CAD API Documentation](https://ssd-api.jpl.nasa.gov/doc/cad.html)
    
- [Astroquery Documentation](https://astroquery.readthedocs.io/)
    
- [SQLAlchemy Documentation](https://docs.sqlalchemy.org/)

