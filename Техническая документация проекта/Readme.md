## 🎯 Назначение проекта

**Asteroid Watch** — это серверная система для автоматического сбора, анализа и мониторинга потенциально опасных астероидов (PHA). Система выполняет следующие задачи:

1. **Автоматический сбор данных** из астрономических каталогов (Minor Planet Center, JPL SBDB)
    
2. **Расчет сближений** астероидов с Землей на 10 лет вперед
    
3. **Оценку уровня угрозы** для каждого сближения на основе диаметра, расстояния и скорости
    
4. **Хранение данных** в структурированной базе данных
    

**Целевая аудитория**: астрономы, исследователи космоса, организации по планетарной защите, образовательные учреждения.

---

## 🚀 Быстрый старт

### Предварительные требования

- **Python 3.10+**
    
- **PostgreSQL 14+**
    
- **Git**
    
- **Учетные записи** для доступа к:
    
    - Minor Planet Center (через `astroquery`)
        
    - JPL Horizons API
        
    - JPL Small-Body Database (SBDB)
        

### Установка

```bash
# Клонирование репозитория
git clone <repository-url>
cd asteroid-watch

# Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установка зависимостей
pip install -r requirements.txt
```

### Настройка окружения

1. Скопируйте файл `.env.example` в `.env`:
    

```bash
cp .env.example .env
```

2. Настройте переменные окружения в `.env`:
    

``` env
# Настройки PostgreSQL
POSTGRES_USER=asteroid_watch
POSTGRES_PASSWORD=your_secure_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=asteroid_watch
POSTGRES_ASYNC_DRIVER=asyncpg
```

3. Создайте базу данных PostgreSQL:

``` bash
createdb asteroid_watch -U postgres
```

4. Примените миграции Alembic:

``` bash
alembic upgrade head
```

### Запуск системы

#### Первоначальное заполнение базы данных

``` bash
python -m utils.write_to_db
```

#### Запуск ежедневного обновления (вручную)

``` bash
python update_db.py
```

