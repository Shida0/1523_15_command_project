# 🌌 **ОБЗОР ПРОЕКТА ASTEROID WATCH**

## 📋 **ЦЕЛЬ И ЗАДАЧИ СИСТЕМЫ**

**Asteroid Watch** - это система мониторинга потенциально опасных астероидов (PHA - Potentially Hazardous Asteroids), которая интегрируется с API NASA для получения актуальных данных об астероидах, сближениях и угрозах столкновений.

### **Основные цели:**
- Мониторинг потенциально опасных астероидов
- Анализ данных о сближениях астероидов с Землей
- Оценка угроз столкновений по шкалам Турина и Палермо
- Предоставление API для доступа к данным

### **Ключевые возможности:**
- Получение данных из NASA SBDB (Small-Body Database), CAD (Close Approach Data) и Sentry API
- Хранение информации об астероидах, их орбитальных параметрах и физических характеристиках
- Анализ данных о сближениях астероидов с Землей
- Оценка потенциальных угроз столкновений
- Предоставление REST API для доступа к данным

## 🛠️ **ТЕХНОЛОГИЧЕСКИЙ СТЕК**

- **Python 3.11+** - основной язык программирования
- **FastAPI** - веб-фреймворк для создания API
- **SQLAlchemy (асинхронный)** - ORM для работы с базой данных
- **PostgreSQL** - основная база данных
- **aiohttp** - асинхронные HTTP-запросы к внешним API
- **Pydantic** - валидация данных
- **Docker** - контейнеризация приложения
- **pytest** - фреймворк для тестирования

## 🎯 **АРХИТЕКТУРНЫЙ ДИЗАЙН**

### **Domain-Driven Design (DDD)**
Проект реализует принципы Domain-Driven Design с тремя основными доменами:
- **asteroid** - управление информацией об астероидах
- **approach** - управление данными о сближениях
- **threat** - управление оценками угроз

Каждый домен содержит свои:
- **models** - SQLAlchemy модели для хранения данных
- **schemas** - Pydantic схемы для валидации
- **repositories** - слой доступа к данным
- **services** - бизнес-логика

### **Паттерн Unit of Work**
Реализован в `shared.transaction.uow.UnitOfWork` для управления транзакциями и сессиями базы данных, что позволяет выполнять несколько операций в рамках одной транзакции и обеспечивает согласованность данных.

### **Многоуровневая архитектура**
```
API Layer → Services → Repositories → Database
```
- **API Layer** - предоставляет интерфейс для взаимодействия
- **Services** - содержат бизнес-логику
- **Repositories** - отвечают за работу с базой данных
- **Database** - хранит данные

### **Общая инфраструктура**
- **Конфигурация** - централизованное управление настройками
- **База данных** - асинхронные сессии и подключения
- **Внешние API** - клиенты для взаимодействия с NASA API
- **Механизмы отказоустойчивости** - цепи сброса, таймауты, повторные попытки
- **Утилиты** - вспомогательные функции и декораторы

## 🧩 **СТРУКТУРА ПРОЕКТА**

```
school_project/
├── api/                      # FastAPI маршруты и зависимости
├── domains/                  # Доменные модули
│   ├── asteroid/             # Домен астероидов
│   │   ├── models/           # Модели SQLAlchemy
│   │   ├── schemas/          # Pydantic схемы
│   │   ├── repositories/     # Репозитории
│   │   ├── services/         # Бизнес-сервисы
│   │   └── controllers/      # Контроллеры (если есть)
│   ├── approach/             # Домен сближений
│   │   ├── models/
│   │   ├── schemas/
│   │   ├── repositories/
│   │   ├── services/
│   │   └── controllers/
│   └── threat/               # Домен угроз
│       ├── models/
│       ├── schemas/
│       ├── repositories/
│       ├── services/
│       └── controllers/
├── shared/                   # Общие компоненты
│   ├── config/               # Управление конфигурацией
│   ├── database/             # Подключение к БД
│   ├── external_api/         # Клиенты внешних API
│   ├── infrastructure/       # Базовые классы
│   ├── models/               # Базовые модели
│   ├── resilience/           # Механизмы отказоустойчивости
│   ├── transaction/          # Управление транзакциями
│   └── utils/                # Вспомогательные утилиты
├── tests/                    # Тесты
│   ├── unit/                 # Модульные тесты
│   ├── integration/          # Интеграционные тесты
│   └── conftest.py           # Общие фикстуры
└── docs/                     # Документация (текущий проект)
```

## 🔄 **ПОТОК ДАННЫХ**

### **Основные потоки:**
1. **Данные астероидов** → NASA SBDB API → `NASASBDBClient` → `AsteroidRepository` → База данных
2. **Данные сближений** → NASA CAD API → `CADClient` → `ApproachRepository` → База данных  
3. **Данные угроз** → NASA Sentry API → `SentryClient` → `ThreatRepository` → База данных

### **Обработка данных:**
- Внешние API запрашиваются через специализированные клиенты
- Данные нормализуются и преобразуются в формат моделей
- Сохраняются через соответствующие репозитории
- Доступны через сервисы для бизнес-логики

## 🛡️ **МЕХАНИЗМЫ ОТКАЗОУСТОЙЧИВОСТИ**

- **Цепи сброса (Circuit Breaker)** - предотвращают каскадные отказы
- **Повторные попытки (Retry)** - с экспоненциальной задержкой
- **Таймауты** - предотвращают зависание запросов
- **Резервирование (Bulkhead)** - изоляция ресурсов
- **Обработка ошибок** - унифицированная обработка исключений

## 🧪 **СТРАТЕГИЯ ТЕСТИРОВАНИЯ**

- **Модульные тесты** - проверяют отдельные функции и методы
- **Интеграционные тесты** - проверяют взаимодействие между компонентами
- **Тестирование API** - проверяют работоспособность эндпоинтов
- **Использование моков** - для изоляции компонентов при тестировании

---

**Следующий раздел:** [МОДУЛЬ АСТЕРОИДОВ](asteroid_domain.md) - подробное описание домена астероидов