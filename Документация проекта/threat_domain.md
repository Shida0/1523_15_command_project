# ⚠️ **МОДУЛЬ УГРОЗ**

## 📋 **НАЗНАЧЕНИЕ И ОТВЕТСТВЕННОСТИ**

Домен угроз (`domains/threat`) отвечает за:
- Хранение и управление информацией об оценках угроз столкновений
- Обработку данных из NASA Sentry API
- Предоставление методов для анализа угроз столкновений
- Интеграцию с доменом астероидов (связь один к одному)

## 🧱 **МОДЕЛЬ THREATASSESSMENTMODEL**

### **Расположение:** `domains/threat/models/threat_assessment.py`

### **Назначение:**
Модель для хранения оценок угроз столкновений из NASA Sentry API, соответствует таблице `threat_assessment_models`.

### **Атрибуты модели:**

| Атрибут | Тип | Назначение |
|---------|-----|------------|
| `id` | `int` | Уникальный идентификатор (автоинкремент) |
| `created_at` | `datetime` | Время создания записи |
| `updated_at` | `datetime` | Время последнего обновления |
| `asteroid_id` | `int` | Ссылка на астероид (One-to-One) |
| `designation` | `str` | Обозначение астероида (например, '2023 DW') |
| `fullname` | `str` | Полное название астероида |
| `ip` | `float` | Вероятность столкновения (impact probability) |
| `ts_max` | `int` | Максимальное значение по Туринской шкале |
| `ps_max` | `float` | Максимальное значение по Палермской шкале |
| `diameter` | `float` | Диаметр астероида в километрах (из Sentry) |
| `v_inf` | `float` | Скорость на бесконечности в км/с |
| `h` | `float` | Абсолютная звездная величина (H) из Sentry |
| `n_imp` | `int` | Количество сценариев столкновения |
| `impact_years` | `List[int]` | Года возможных столкновений (JSON-массив) |
| `last_obs` | `str` | Дата последнего наблюдения |
| `threat_level_ru` | `str` | Уровень угрозы на русском |
| `torino_scale_ru` | `Text` | Описание по Туринской шкале на русском |
| `impact_probability_text_ru` | `Text` | Текст вероятности на русском |
| `energy_megatons` | `float` | Энергия воздействия в мегатоннах тротила |
| `impact_category` | `str` | Категория воздействия: 'локальный', 'региональный', 'глобальный' |
| `sentry_last_update` | `datetime` | Время последнего обновления данных из Sentry API |

### **Отношения:**
- `asteroid`: Связь один к одному с `AsteroidModel`

### **Методы модели:**
- `__init__(**kwargs)` - инициализирует экземпляр модели оценки угрозы с валидацией и нормализацией данных
- `_calculate_energy() -> float` - расчет энергии удара в мегатоннах тротила
- `_determine_impact_category() -> str` - определение категории воздействия на основе энергии
- `_assess_threat_level() -> str` - оценка уровня угрозы на основе шкал Турина и Палермо
- `__repr__()` - возвращает строковое представление объекта

## 📐 **СХЕМЫ PYDANTIC**

### **ThreatAssessmentBase**
**Расположение:** `domains/threat/schemas/threat_schema.py`

**Назначение:** Базовая схема оценки угрозы с общими полями.

**Атрибуты:**
- `id` (int): Уникальный идентификатор
- `created_at` (datetime): Время создания
- `updated_at` (datetime): Время обновления
- `asteroid_id` (int): ID астероида
- `designation` (str): Обозначение астероида
- `fullname` (str): Полное имя астероида
- `ip` (float): Вероятность столкновения (0.0-1.0)
- `ts_max` (int): Туринская шкала (0-10)
- `ps_max` (float): Палермская шкала
- `diameter_km` (float): Диаметр в км (≥0.0)
- `velocity_km_s` (float): Скорость в км/с (≥0.0)
- `absolute_magnitude` (float): Абсолютная звездная величина
- `n_imp` (int): Количество сценариев (≥0)
- `impact_years` (List[int]): Года возможных столкновений
- `last_obs` (str): Дата последнего наблюдения
- `threat_level_ru` (str): Уровень угрозы на русском
- `torino_scale_ru` (str): Описание по Туринской шкале на русском
- `impact_probability_text_ru` (str): Текст вероятности на русском
- `energy_megatons` (float): Энергия в мегатоннах (≥0.0)
- `impact_category` (str): Категория воздействия

### **ThreatAssessmentCreate**
**Назначение:** Схема для создания оценки угрозы.

**Атрибуты:**
- Все атрибуты из `ThreatAssessmentBase` плюс:
- `energy_megatons` (float): Энергия в мегатоннах с дефолтным значением 0.0
- `impact_category` (str): Категория воздействия с дефолтным значением "локальный"

### **ThreatAssessmentResponse**
**Назначение:** Схема для ответа API с оценкой угрозы.

**Атрибуты:** Все атрибуты из `ThreatAssessmentBase`

## 🏪 **РЕПОЗИТОРИЙ THREATREPOSITORY**

### **Расположение:** `domains/threat/repositories/threat_repository.py`

### **Назначение:**
Репозиторий для операций с оценками угроз (One-to-One), наследует `BaseRepository` и предоставляет специализированные методы для работы с `ThreatAssessmentModel`.

### **Методы репозитория:**

#### **`get_by_designation(designation: str) -> Optional[ThreatAssessmentModel]`**
- **Назначение:** Получает оценку угрозы по обозначению астероида
- **Параметры:** `designation` - обозначение астероида
- **Возвращает:** `Optional[ThreatAssessmentModel]` - оценка угрозы или None

#### **`get_by_asteroid_id(asteroid_id: int) -> Optional[ThreatAssessmentModel]`**
- **Назначение:** Получает оценку угрозы для конкретного астероида (One-to-One)
- **Параметры:** `asteroid_id` - ID астероида
- **Возвращает:** `Optional[ThreatAssessmentModel]` - оценка угрозы или None

#### **`get_high_risk_threats(limit: int = 20) -> List[ThreatAssessmentModel]`**
- **Назначение:** Получает угрозы с высоким уровнем риска (ts_max >= 5)
- **Параметры:** `limit` - максимальное количество
- **Возвращает:** `List[ThreatAssessmentModel]` - список угроз

#### **`get_threats_by_risk_level(min_ts: int = 0, max_ts: int = 10, skip: int = 0, limit: int = 100) -> List[ThreatAssessmentModel]`**
- **Назначение:** Получает угрозы по диапазону значений Туринской шкалы
- **Параметры:** `min_ts`, `max_ts`, `skip`, `limit`
- **Возвращает:** `List[ThreatAssessmentModel]` - список угроз

#### **`get_threats_by_probability(min_probability: float = 0.0, max_probability: float = 1.0, skip: int = 0, limit: int = 100) -> List[ThreatAssessmentModel]`**
- **Назначение:** Получает угрозы по диапазону вероятности столкновения
- **Параметры:** `min_probability`, `max_probability`, `skip`, `limit`
- **Возвращает:** `List[ThreatAssessmentModel]` - список угроз

#### **`get_threats_by_energy(min_energy: float = 0.0, max_energy: Optional[float] = None, skip: int = 0, limit: int = 100) -> List[ThreatAssessmentModel]`**
- **Назначение:** Получает угрозы по диапазону энергии воздействия
- **Параметры:** `min_energy`, `max_energy`, `skip`, `limit`
- **Возвращает:** `List[ThreatAssessmentModel]` - список угроз

#### **`get_threats_by_impact_category(category: str, skip: int = 0, limit: int = 100) -> List[ThreatAssessmentModel]`**
- **Назначение:** Получает угрозы по категории воздействия
- **Параметры:** `category` - категория воздействия
- **Возвращает:** `List[ThreatAssessmentModel]` - список угроз

#### **`update_threat_assessment(designation: str, new_data: Dict[str, Any]) -> Optional[ThreatAssessmentModel]`**
- **Назначение:** Обновляет оценку угрозы для астероида с коммитом
- **Параметры:** `designation`, `new_data` - новые данные
- **Возвращает:** `Optional[ThreatAssessmentModel]` - обновленная оценка или None

#### **`bulk_create_threats(threats_data: List[Dict[str, Any]]) -> Tuple[int, int]`**
- **Назначение:** Массовое создание оценок угроз (One-to-One) с коммитом
- **Параметры:** `threats_data` - список данных угроз
- **Возвращает:** `Tuple[int, int]` - количество созданных и обновленных

#### **`get_statistics() -> Dict[str, Any]`**
- **Назначение:** Возвращает статистику по оценкам угроз
- **Возвращает:** `Dict[str, Any]` - словарь со статистикой

#### **`search_threats(search_term: str, skip: int = 0, limit: int = 50) -> List[ThreatAssessmentModel]`**
- **Назначение:** Ищет угрозы по обозначению или полному названию астероида
- **Параметры:** `search_term`, `skip`, `limit`
- **Возвращает:** `List[ThreatAssessmentModel]` - список угроз

## 🛠️ **СЕРВИС THREATSERVICE**

### **Расположение:** `domains/threat/services/threat_service.py`

### **Назначение:**
Сервис для работы с оценками угроз, предоставляет бизнес-логику для анализа угроз столкновений.

### **Атрибуты:**
- `session_factory`: Фабрика для создания сессий SQLAlchemy

### **Методы сервиса:**

#### **`get_by_designation(designation: str) -> Optional[Dict[str, Any]]`**
- **Назначение:** Получение оценки угрозы по обозначению астероида
- **Параметры:** `designation` - обозначение астероида в системе NASA
- **Возвращает:** `Optional[Dict[str, Any]]` - словарь с данными об оценке угрозы или None
- **Пример:**
```python
threat = await service.get_by_designation("433")
if threat:
    print(f"Угроза для 433: Туринская шкала = {threat['ts_max']}")
```

#### **`get_by_asteroid_id(asteroid_id: int) -> Optional[Dict[str, Any]]`**
- **Назначение:** Получение оценки угрозы для астероида по его ID
- **Параметры:** `asteroid_id` - уникальный идентификатор астероида
- **Возвращает:** `Optional[Dict[str, Any]]` - словарь с данными об оценке угрозы или None

#### **`get_high_risk(limit: int = 20) -> List[Dict[str, Any]]`**
- **Назначение:** Получение угроз с высоким уровнем риска (туринская шкала >= 5)
- **Параметры:** `limit` - максимальное количество возвращаемых угроз (по умолчанию 20)
- **Возвращает:** `List[Dict[str, Any]]` - список угроз с высоким уровнем риска

#### **`get_by_risk_level(min_ts: int = 0, max_ts: int = 10) -> List[Dict[str, Any]]`**
- **Назначение:** Получение угроз по диапазону значений Туринской шкалы
- **Параметры:** `min_ts` - минимальное значение по Туринской шкале (по умолчанию 0), `max_ts` - максимальное значение по Туринской шкале (по умолчанию 10)
- **Возвращает:** `List[Dict[str, Any]]` - список угроз в заданном диапазоне значений Туринской шкалы

#### **`get_statistics() -> Dict[str, Any]`**
- **Назначение:** Возвращает статистику по оценкам угроз астероидов
- **Возвращает:** `Dict[str, Any]` - словарь со статистическими данными об оценках угроз

#### **`get_by_energy(min_energy: float = 0.0, max_energy: Optional[float] = None) -> List[Dict[str, Any]]`**
- **Назначение:** Получение угроз по диапазону энергии воздействия
- **Параметры:** `min_energy` - минимальная энергия воздействия в мегатоннах (по умолчанию 0.0), `max_energy` - максимальная энергия воздействия в мегатоннах (по умолчанию None)
- **Возвращает:** `List[Dict[str, Any]]` - список угроз в заданном диапазоне энергии воздействия

#### **`get_by_category(category: str) -> List[Dict[str, Any]]`**
- **Назначение:** Получение угроз по категории воздействия
- **Параметры:** `category` - категория воздействия (например, "Mercury", "Venus", "Earth", "Mars", "Jupiter")
- **Возвращает:** `List[Dict[str, Any]]` - список угроз указанной категории воздействия

#### **`_model_to_dict(model_instance) -> Dict[str, Any]`**
- **Назначение:** Преобразование экземпляра модели в словарь

## 🧪 **ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ**

### **Пример использования репозитория:**
```python
from shared.transaction.uow import UnitOfWork
from shared.database.engine import AsyncSessionLocal

# Использование репозитория в рамках Unit of Work
async with UnitOfWork(AsyncSessionLocal) as uow:
    # Получить оценку угрозы по обозначению
    threat = await uow.threat_repo.get_by_designation("433")
    
    # Найти угрозы с высоким риском (туринская шкала >= 5)
    high_risk = await uow.threat_repo.get_high_risk_threats(20)
    
    # Найти угрозы по диапазону вероятности
    probable_threats = await uow.threat_repo.get_threats_by_probability(
        min_probability=0.001, max_probability=0.01
    )
    
    # Получить статистику по угрозам
    stats = await uow.threat_repo.get_statistics()
    
    print(f"Всего угроз: {stats['total_threats']}")
    print(f"Угроз высокого риска: {stats['high_risk_count']}")
```

### **Пример использования сервиса:**
```python
from domains.threat.services.threat_service import ThreatService
from shared.database.engine import AsyncSessionLocal

# Использование сервиса
service = ThreatService(AsyncSessionLocal)

# Получить оценку угрозы по обозначению
threat = await service.get_by_designation("433")
if threat:
    print(f"Угроза для 433: Туринская шкала = {threat['ts_max']}")

# Получить угрозы с высоким риском
high_risk = await service.get_high_risk(10)
print(f"Угрозы высокого риска: {len(high_risk)}")

# Получить угрозы по диапазону значений Туринской шкалы
medium_risk = await service.get_by_risk_level(2, 4)
print(f"Угрозы среднего риска (2-4): {len(medium_risk)}")

# Получить угрозы по энергии воздействия
high_energy_threats = await service.get_by_energy(100.0, 1000.0)
print(f"Угрозы с энергией 100-1000 Мт: {len(high_energy_threats)}")

# Получить статистику
stats = await service.get_statistics()
print(f"Всего оценок угроз: {stats['total_threats']}")
print(f"Угроз высокого риска: {stats['high_risk_count']}")
```

### **Пример массового создания угроз:**
```python
from shared.transaction.uow import UnitOfWork
from shared.database.engine import AsyncSessionLocal

# Подготовка данных для массового создания
threats_data = [
    {
        "asteroid_id": 1,
        "designation": "433",
        "fullname": "Eros",
        "ip": 0.0001,
        "ts_max": 1,
        "ps_max": -3.5,
        "diameter": 0.17,
        "v_inf": 15.5,
        "h": 11.17,
        "n_imp": 1,
        "impact_years": [2025],
        "last_obs": "2023-05-15",
        "threat_level_ru": "ОЧЕНЬ НИЗКИЙ",
        "torino_scale_ru": "1 — Нормальный (зелёный)",
        "impact_probability_text_ru": "0.01% (1 к 10,000)",
        "energy_megatons": 125.5,
        "impact_category": "локальный",
        "sentry_last_update": "2023-05-15T10:00:00Z"
    },
    {
        "asteroid_id": 2,
        "designation": "495",
        "fullname": "Eureka",
        "ip": 0.00005,
        "ts_max": 0,
        "ps_max": -4.2,
        "diameter": 0.08,
        "v_inf": 12.0,
        "h": 14.5,
        "n_imp": 1,
        "impact_years": [2027],
        "last_obs": "2023-04-20",
        "threat_level_ru": "НУЛЕВОЙ (ниже фонового уровня)",
        "torino_scale_ru": "0 — Нет риска (зелёный)",
        "impact_probability_text_ru": "0.005% (1 к 20,000)",
        "energy_megatons": 25.3,
        "impact_category": "локальный",
        "sentry_last_update": "2023-04-20T10:00:00Z"
    }
]

async with UnitOfWork(AsyncSessionLocal) as uow:
    created, updated = await uow.threat_repo.bulk_create_threats(threats_data)
    print(f"Создано угроз: {created}, Обновлено: {updated}")
```

### **Пример обновления оценки угрозы:**
```python
from shared.transaction.uow import UnitOfWork
from shared.database.engine import AsyncSessionLocal

async with UnitOfWork(AsyncSessionLocal) as uow:
    # Обновить данные оценки угрозы
    updated_threat = await uow.threat_repo.update_threat_assessment(
        designation="433",
        new_data={
            "ip": 0.00005,  # Обновленная вероятность
            "ts_max": 0,    # Обновленная шкала Турина
            "sentry_last_update": "2023-06-01T10:00:00Z"
        }
    )
    
    if updated_threat:
        print(f"Оценка угрозы для {updated_threat.designation} обновлена")
    else:
        print("Оценка угрозы не найдена для обновления")
```

---

**Следующий раздел:** [ОБЩИЕ КОМПОНЕНТЫ](shared_components.md) - описание конфигурации, базы данных и внешних API